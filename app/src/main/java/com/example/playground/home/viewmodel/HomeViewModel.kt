package com.example.playground.home.viewmodel

import androidx.lifecycle.ViewModel
import javax.inject.Inject

class HomeViewModel @Inject constructor(): ViewModel() {
    val name = "HomeViewModel"
}